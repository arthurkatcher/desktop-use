# session recordings: screenshots + event logs of the machine
sessions/

# screen registry + local settings (operator machine state)
screens/
settings.json

# secrets and local env (never commit)
.env
.env.*
!.env.example
*.key
*.pem
credentials.json

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# editors / os
.idea/
.vscode/
*.swp
.DS_Store

# local machine notes, never commit
*.local.md
